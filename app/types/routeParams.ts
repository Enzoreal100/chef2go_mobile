import { HeaderProps } from '@/components/header/headerProps.interface';

// Interface para os parâmetros da rota 'index'
export interface IndexRouteParams {
  headerProps: HeaderProps;
  // Adicione outros parâmetros específicos para a rota 'index' se houver
}